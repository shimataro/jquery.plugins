/*!
 * Skelton of jQuery plugin
 * requires:
 *  jQuery x.x.x+
 *  other libraries...
 */
(function($, window)
{
	$.fn.extend({
		/**
		 * description
		 * @function
		 */
		skelton: (function($)
		{
			return function()
			{
				// return "this" to enable chain methods
				return this;
			};
		})($)
	});
})(jQuery, window);
