/*!
 * Skelton of jQuery plugin
 * requires:
 *  jQuery x.x.x+
 *  other libraries...
 */
(function($, window)
{
	$.fn.extend({
		skelton: (function($)
		{
			/**
			 * description
			 * @function
			 */
			return function()
			{
				return this;
			};
		})($)
	});
})(jQuery, window);
